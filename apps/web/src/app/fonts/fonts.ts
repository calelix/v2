import {
  Geist,
  Geist_Mono,
} from "next/font/google"

export const fontGeist = Geist({
  subsets: ["latin"],
  variable: "--font-sans",
})

export const fontGeistMono = Geist_Mono({
  subsets: ["latin"],
  variable: "--font-mono",
})

